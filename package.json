{
  "name": "fck-honey",
  "version": "0.1.0",
  "description": "Detects Honey browser extension overlays for merchants.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
